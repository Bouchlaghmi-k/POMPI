terraform {
  required_version = ">= 1.5.0"
}

# Base Terraform initiale pour le projet POMPI
# Ce fichier sera enrichi plus tard pour provisionner
# l'infrastructure et les composants nécessaires au projet.